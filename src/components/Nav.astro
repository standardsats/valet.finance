---
import { site } from '../data/site.ts';
const { nav } = site;
---

<header class="wrap">
  <nav class="nav">
    <a class="brand" href="#">
      <img class="logo" src="/assets/valet-icon.png" alt="Valet">
      <span class="word">{nav.brand}</span>
    </a>
    <div class="nav-links">
      {nav.links.map((link) => (
        <a href={link.href}>{link.label}</a>
      ))}

      <a class="nav-cta" href={nav.cta.href}>{nav.cta.label}</a>
    </div>
  </nav>
</header>
